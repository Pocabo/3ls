# 변경 이력 (Changelog)

## 버전 규칙

3Ls는 **LANG.MAJOR.MINOR.PATCH** 네 자리 버전을 씁니다. **LANG 자리만 봐도 지원 언어 개수를 알 수 있게** 설계했습니다.

| 자리 | 올리는 기준 | 값의 의미 / 예시 |
|---|---|---|
| **LANG** | **새 언어**를 추가 | 값 = 지원 언어 개수. 현재 4 (프랑스어·스페인어·일본어·한자어) |
| **MAJOR** | 대형 기능/**새 콘텐츠 축**이 생김 — 앱 전체에 걸친 기능 메뉴, 또는 "English→French" 같은 새 학습 방향. **LANG이 오르면 0으로 리셋** | 📚 단어은행 신설 |
| **MINOR** | 메뉴 추가 없이 **UI가 바뀌어 UX가 달라짐**. **MAJOR가 오르면 0으로 리셋** | 다크모드, 열 접기, 어절 단위 줄바꿈, Contents 드롭다운·언어 선택 페이지 |
| **PATCH** | UI 오류·버그 수정, 또는 **기존 언어에 콘텐츠/단어 추가**. **MINOR가 오르면 0으로 리셋** | 발음 열 줄바꿈 수정, 한자어 단어 추가, `fr-extra` 확장 팩 추가 |

- **왼쪽 자리가 오르면 그 오른쪽 자리는 모두 0으로 리셋**합니다 (예: 새 언어 추가 → `x.0.0.0`).
- 기존 언어의 **확장/사전 팩**(예: `fr-extra`, `parent:'fr'`)은 새 언어가 아니므로 LANG·MAJOR를 올리지 않습니다. 언어 선택 화면엔 뜨지 않고, 상단 **📂 Contents** 드롭다운에서 접근합니다.
- **⚠️ 팩 규격 변경**: 언어팩 데이터 구조(필드 추가/의미 변경, 예: `tip` 필드 도입, `parent`/`menuLabel` 필드, 열 자동 숨김 규칙)가 바뀌면 자리와 별개로 해당 항목에 `⚠️ 팩 규격 변경` 을 표기해, 나중에 팩을 만들 때 맞춰야 할 규격을 한눈에 보이게 합니다.
- 참고: v4.1.0.0 이전 항목은 옛 2~3자리 번호 체계로 기록돼 있습니다(역사 보존용). 아래 v4.1.0.0부터 이 4자리 규칙을 적용합니다.
- 새 버전을 낼 때는 **`index.html`의 `APP_VERSION`**, **`service-worker.js`의 `CACHE`**, **`manifest.json`의 `version`**, **`README.md`의 현재 버전** 을 모두 같은 값으로 맞춥니다(하나만 바꾸면 화면 표기·캐시·문서가 어긋납니다).
- `manifest.json`의 `version` 은 브라우저가 읽지 않는 참고용 표기입니다.

---
## v7.1.2.0 — 2026-09-16
**MINOR · 세로쓰기(縦書き) 전환 + 青空文庫 변환기와 일본어 원서 두 편**

### 📜 세로쓰기
- 읽기 탭에 **세로쓰기 / 가로쓰기** 버튼이 생겼습니다. 한중일 고전을 인쇄된 모양대로 읽습니다
- 위에서 아래로 흐르고 단이 오른쪽에서 왼쪽으로 쌓입니다. 본문이 제 몸통 안에서 가로로 넘어가고 페이지는 안 밀립니다
- **한중일 글에만 버튼이 뜹니다.** 팩에 표시를 요구하지 않고 본문 앞부분을 훑어 정합니다 — 엔진이 언어를 모른다는 원칙 그대로. 라틴어를 열면 버튼이 사라집니다
- 두 자 이하 표지는 나란히 세웁니다(縦中横)
- 선택은 기기에 저장되고, 전환해도 읽던 자리를 지킵니다

물리 속성을 논리 속성으로 바꿔(`border-block-end`·`padding-inline`·`margin-inline-start`) `writing-mode` 하나로 여백과 구분선이 따라 돌게 했습니다.

⚠️ 세로쓰기에서 **block 축은 가로**입니다. 높이를 잡으려면 `block-size` 가 아니라 물리 속성 `height` 를, 폭을 가두려면 `width` 를 명시해야 합니다. `block-size:70vh` 를 쓰면 높이가 아니라 폭이 잡혀 본문이 아래로만 쌓입니다.

⚠️ `text-combine-upright: digits 2` 는 Chrome 이 구현하지 않았습니다(`all` 만 먹습니다). 표지 길이를 렌더할 때 재서 두 자 이하일 때만 `.n2` 를 붙입니다 — `1:31` 같은 긴 표지를 뭉개지 않게.

**축이 바뀌면서 같이 손본 것**
- 읽던 위치 추적이 축을 가립니다 — 가로는 페이지 스크롤, 세로는 본문 스크롤. 세로에서는 아직 오른쪽 끝을 안 지난 첫 단을 찾습니다
- 줄 이동은 `scrollIntoView({block:'center', inline:'nearest'})` 로 양쪽 축에서 같이 동작합니다 (측정: 착지 오차 1px)

### 📚 青空文庫 변환기
- `tools/aozora.py` — 청공문고 텍스트(Shift-JIS)를 서재 언어팩으로
  - `漢字《かんじ》` 루비, `｜` 범위 지정, `［＃…］` 입력자 주를 각각 풀어냅니다
  - **본문**은 루비를 뗀 원문, **발음(say)** 은 루비를 끼운 문장 → TTS 가 어려운 한자를 바로 읽습니다
  - **용어집**은 한자 → 요미 표. 본문에서 한자를 더블클릭하면 읽는 법이 뜹니다
  - 루비는 첫 등장에만 붙으므로, 두 글자 이상 한자는 용어집으로 뒤쪽까지 메웁니다
    (한 글자는 제외 — `反 → かえ` 를 적용하면 `反対` 가 `かえ対` 가 됩니다)
  - `--flat` — 장면이 잘게 나뉜 글을 한 편으로 합칩니다. 시나리오 78장면이 목차 항목 78개가 되는 걸 막습니다

### 수록 자료 — 芥川龍之介 두 편 (공유 저작물)
- **アグニの神** — 6장 195줄, 용어집 144, 발음 보강 81곳
- **浅草公園 ―― 或シナリオ ――** — 78장면 325줄(한 편으로 합침), 용어집 175, 발음 보강 105곳

### 검증
- 22개 팩 × 주요 탭 = **100회 렌더 전부 통과**, 콘솔 오류 0
- 세로 전환 → 줄 이동(오차 1px) → 위치 저장 → 가로 복귀 → 재전환 전 경로 확인
- 라틴어는 세로쓰기 설정이 켜져 있어도 가로 유지, 버튼 미표시
- 데스크톱(1000×820)·모바일(375×812) 둘 다 본문 안에서만 가로 스크롤, 페이지는 안 밀림
- TTS 가 루비 치환문을 읽는 것 확인 (`支那の上海の或町です。` → `シナのシャンハイのある町です。`, ja-JP)

---
## v7.1.1.0 — 2026-09-16
**MINOR · 읽다가 만난 낱말을 복습으로 + 🇨🇳 중국어 10레벨 완성**

### 📜 읽기 → 🔁 복습 연결
- 낱말 뜻 풍선에 **＋ 복습** 버튼이 생겼습니다. 누르면 그 낱말이 복습(SRS) 루프에 바로 들어갑니다
- 담기는 뜻은 **풍선이 보여준 것 그대로**입니다 — 줄 주석이든 글 용어집이든 다른 팩 사전이든
- 이미 담은 낱말은 풍선을 다시 열면 `✓ 담음`으로 뜹니다
- 담은 낱말은 `S.langs[팩].saved` 에 따로 쌓이고 `allWords()` 와 분리해 뒀습니다
  — 홈 화면의 "단어 N개 중 M개" 같은 **커리큘럼 진도 표시가 읽기 낱말로 부풀지 않게** 하기 위해서입니다
  복습 대상(`dueWords()`)에만 합류합니다
- SRS 키는 `RD|<정규화한 낱말>` 이라 팩 단어와 절대 겹치지 않습니다

이걸로 **읽기와 암기가 이어집니다.** 전에는 읽다가 모르는 낱말을 만나도 뜻만 보고 지나갔습니다.

### 🇨🇳 중국어 6~10레벨 — 10레벨 과정 완성
5레벨에서 끊겨 있던 유일한 본과정을 다른 언어와 같은 10레벨로 맞췄습니다. **120단어**(이전 60).

- **L6 길찾기와 교통** — 怎么로 길 묻기, 坐 + 교통수단, `从…到…` ↔ `离` 구별
- **L7 지난 일과 경험** — 了(완료·변화)와 过(경험). `没`와 `了`가 공존하지 않는다는 규칙
- **L8 비교와 몸 상태** — `A 比 B + 형용사`(很 금지), 有点儿 ↔ 一点儿 위치가 반대
- **L9 할 수 있다와 계획** — 会·能·可以 세 갈래, 짝 접속사 `因为…所以` `虽然…但是`
- **L10 把·被와 보어** — 목적어를 앞으로 빼는 把(결과 필수), 피동 被, 결과보어·정도보어 得

### 검증
- 중국어 10레벨 × 5탭 = **50회 렌더 전부 통과**, 콘솔 오류 0
- 읽기 → 담기 → 복습 화면 등장 → 재조회 시 `✓ 담음` 전 경로 확인
- 홈 화면 단어 수가 담은 낱말로 부풀지 않는 것 확인 (라틴어 286 유지)

---
## v7.1.0.1 — 2026-09-08
**PATCH · 🏛 라틴어 7~16레벨 — 16단계 과정 완성**

v7.0.0.0 에서 1~6레벨만 내놓았던 라틴어 과정을 계획대로 끝까지 채웠습니다.
**16레벨 · 286단어 · 읽기 50줄** (이전 6레벨 · 102단어 · 21줄).

### 동사 체계 (L7~L11 · phase `verb`)
- **L7 나머지 세 활용** — 2·3·3(-iō)·4활용 현재, 주요부 넷의 역할, 부정사·명령법
- **L8 미완료·미래** — 표지 -bā-, 미래가 활용마다 다른 이유(1·2활용 -bi-, 3·4활용 -a-/-ē-)
- **L9 완료 계열** — 완료·과거완료·미래완료. 여섯 시제를 어간 둘로 갈라 정리
- **L10 수동태** — 현재 계열은 어미 교체, 완료 계열은 분사 + sum. 행위자 ā/ab ↔ 맨 탈격
- **L11 탈형·불규칙동사** — sequor·loquor 무리, possum·eō·ferō·volō/nōlō/mālō·fīō

### 구문 (L12~L14 · phase `synt`)
- **L12 비교·부사·수사** — -ior/-issimus, 불규칙 다섯, quam ↔ 비교의 탈격, 부사 -ē/-iter
- **L13 분사·탈격 절대구** — 분사 넷과 상대 시간, 동형사의 의무, absolute 구조
- **L14 대격+부정사(AcI)** — 간접화법의 뷈대, 부정사 시제의 상대성, sē가 가리키는 사람

### 접속법 (L15~L16 · phase `high`)
- **L15 형태와 주절 용법** — 네 시제 만드는 법, 권유·소망·숙고·완공한 명령, 부정어 nē
- **L16 종속절** — 목적절·결과절(부정으로 갈린다), cum절 세 가지, 간접의문, 시제 일치, 조건문 네 유형

### 읽기 지문 셋
- **L9** Prīma victōria — 미완료·완료·과거완료가 어떻게 층을 이루는지 (지은 지문)
- **L14** 옛사람들이 세계를 두고 한 말 — 줄마다 AcI (지은 지문, 6행은 AcI가 아닌 함정)
- **L16** Sī tacuissēs — 접속법이 걸린 고전·중세 겪언 (공유 저작물)

### 발음 표기(say) 자동 생성
- 새 낱말 184개·예문 184개·읽기줄 29개의 `say`/`exSay` 를 변환기로 넣었습니다
- 변환기는 먼저 기존 1~6레벨의 `say` **204건과 대조해 불일치 0**을 확인한 뒤 썼습니다
  — 손으로 400개를 적으면 반드시 오자가 납니다

### 검증
- 16레벨 × 6탭 = **96회 렌더 전부 통과**, 콘솔 오류 0
- 낱말 더블클릭 조회가 새 단어에도 적용됨

---
## v7.1.0.0 — 2026-09-07
**MAJOR · 📜 장문 읽기 확장 + 📚 서재(원서 선택 받기)** — ⚠️ 팩 규격 변경

원문 소설·경전처럼 **긴 글을 웹소설 읽듯** 읽을 수 있게 읽기 포맷을 넓히고,
장편을 앱에 미리 담지 않고 **필요한 것만 골라 받는** 서재를 만들었습니다.

### 📜 읽기 포맷 확장 (⚠️ 팩 규격)
- **`reading` 이 배열을 받습니다** — 레벨 하나에 여러 편(장). 읽기 탭 위에 **목차 드롭다운**이 생기고 편마다 읽은 비율이 붙습니다
  - 장이 아주 많은 자료에서 레벨 카드가 수백 개로 불어나는 걸 막습니다
  - 기존 객체 하나짜리 `reading` 은 그대로 동작합니다(la.js L4~L6 확인)
- **`n` — 줄 표지** — `n:'1:3'` 처럼 절·행 번호를 원문 앞에 작게 붙입니다. 표지로도 검색됩니다
- **`gloss` — 글 전용 용어집** — `gloss:{'rosam':'rosa 장미 (단수 대격)'}`. 굴절형을 담는 자리입니다
- **낱말 더블클릭 → 뜻 풍선** — 찾는 순서는 ① 줄의 `note` ② 글의 `gloss` ③ 설치된 모든 팩의 표제어(정확히 일치)
  - 셋 다 없으면 **없다고 말하고** 통합검색 버튼을 줍니다 — 뜻을 지어내지 않습니다
  - 낱말마다 `<span>`을 감싸지 않습니다(3,000줄이면 노드가 3만 개). 더블클릭은 브라우저가 낱말을 선택해 주므로 selection만 읽으면 **DOM 추가가 0**이고, 한 번 클릭(번역 펼치기)과도 겹치지 않습니다
  - 측정: 굴절어는 ③만으로는 반쪽입니다 — 라틴어 읽기 본문 낱말 76개 중 표제어 일치 34개(45%). `amat`·`vēnit`·`rosam` 은 안 잡힙니다. 접두 매칭 폴백도 재봤지만 미스 42개 중 1개만 건져 **버렸습니다**(오답을 만드느니 없다고 하는 편이 낫습니다). 원문을 낱말까지 다 읽히려면 `gloss` 를 채우세요
- **이어읽기** — 읽던 줄과 글자 크기를 기기에 저장해 다음에 그 자리에서 시작합니다. 첫 줄이 아니면 "이어서 읽기" 배너가 뜹니다
- **글 안 검색** — 읽기 탭의 검색창은 **그 글에서만** 찾습니다. 헤더의 🔎 통합검색(단어)과 인덱스가 분리돼 결과가 섞이지 않습니다
  - 인덱스를 안 만듭니다 — 3,000줄 한 번 훑는 데 **4ms**라 필요가 없습니다
- **글자 크기** — A− / A＋ 로 조절, 기기에 저장

### 📚 서재 (⚙ 메뉴 → 서재)
- `library/index.json` 의 **목록만** 서재 화면에서 읽고, **[받기]를 누른 원서만** 내려받습니다. 앱을 켜는 것만으로는 한 바이트도 받지 않습니다
- 받은 파일은 **버전이 안 붙은 `3ls-library` 캐시**에 넣어 **앱을 업데이트해도 남습니다**
  - 그냥 두면 버전 캐시(`3ls-v…`)에 쌓여 다음 업데이트의 옛 캐시 청소에 통째로 지워졌을 자리입니다. 서비스워커 `activate` 의 보존 목록과 `fetch` 의 `/library/` 통과 규칙이 이걸 막습니다
  - 확인함: 옛 버전 캐시를 지우는 청소를 돌려도 받아둔 파일이 그대로 남음
- **[삭제]** 로 기기에서 지우고 언제든 다시 받습니다. 지운 것이 보던 팩이면 남은 팩으로 자동 이동
- 오프라인에서도 받아둔 원서는 열립니다(부팅 때 캐시에서 되살림)
- 서재 파일은 **그냥 언어팩 JSON** 입니다 — 목차·읽기·검색 등 기존 기능이 그대로 붙습니다
- `tools/mkreading.py` — 평문 텍스트(.txt/.md)를 서재 언어팩으로 바꿔 줍니다. 번역·주석은 **만들지 않습니다**(지어내면 안 되므로 빈 자리로 둡니다)
  - **쪼개기 세 가지** — `line`(성경·시) / `sentence`(소설) / `para`(수필). 문장 분할은 `Mr.` `Dr.` `J. R. R.` 같은 약어·이니셜을 피하고, 한국어(`.`)·일본어·중국어(`。`)도 처리합니다
  - 마크다운 인라인 표기(`**굵게**` · `[글자](주소)` · 인용 · 목록 · 구분선)를 벗겨냅니다
  - `--index` 로 `library/index.json` 을 자동 갱신 — 줄 수·용량을 손으로 옮겨 적지 않아도 됩니다
- `tools/mkreading_gui.pyw` — **더블클릭으로 여는 변환 창**(tkinter, 표준 라이브러리만 씁니다)
  - **미리보기에서 교정** — 자동 문장 분할은 반드시 틀리므로 줄 합치기·나누기·고치기가 이 창의 존재 이유입니다
  - **적중률 점검** — 본문 낱말 중 몇 %에 뜻이 붙는지 세고, 빠진 형태를 빈도순 빈 표(TSV)로 뽑아 줍니다
  - exe 로 안 만들었습니다 — PyInstaller onefile 은 10~15 MB에 백신 오탐이 있고, 만든 JSON 을 어차피 이 저장소에 올려야 해서 실익이 없습니다
- `tools/원서-작성법.md` — 텍스트 파일 적는 법, 장단점과 실전 예(성경·소설·시), 자주 걸리는 것
  - `--gloss` 로 용어집 TSV를 붙입니다. 용어집은 **팩 단위로 한 번만** 들어갑니다 — 편마다 복사하면 50장짜리에서 같은 표가 50번 들어갑니다
  - 조회 순서는 줄의 `note` → 편의 `gloss` → 팩의 `gloss` → 설치된 사전

### 수록 자료 — 불가타 창세기 1장
- `library/la-vulgata-genesis.json` — 라틴어 불가타 성경 창세기 1장 31절 (공유 저작물)
- 절 번호를 `n:'1:1'` 표지로 달아 그 번호로 바로 찾을 수 있습니다
- **용어집 213항목 — 본문 낱말 전수 수록.** 표제어만이 아니라 본문에 나오는 굴절형 그대로 담았습니다
  - 검산: 본문 낱말 **538연 중 538 적중(100%)**, 용어집에만 있고 본문에 없는 항목 0개
  - 용어집 없이 설치된 사전만으로는 **23%**만 잡혔습니다(`la.js` 표제어 123개 대비). 산문 원전은 굴절형이 대부분이라 용어집이 사실상 필수입니다

### 엔진 정비
- **`content-visibility:auto`** — 화면 밖 줄의 레이아웃을 브라우저가 건너뜁니다. 3,000줄에서 줄 이동이 **541ms → 7~36ms**
  - `scrollIntoView` 를 두 번 부릅니다. 화면 밖 높이는 추정값이라 한 번으로는 어긋납니다(측정: 착지 오차 27~37px, 한 줄 높이 52px 미만)
- **단어 없는 팩 지원** — `words` 없이 `reading` 만 있는 팩이 정상 동작합니다
  - `isEmptyPack` 이 읽기 자료도 "내용 있음"으로 셉니다(전에는 빈 껍데기로 보고 메뉴에서 숨겼습니다)
  - `packBases` 가 읽기 줄의 `m` 키도 base 로 인정합니다
  - **탭이 내용 있는 것만 뜹니다** — 전에는 읽기만 있는 팩에도 암기카드·문법·연습·시험 탭 4개가 빈 채로 떴습니다
  - 홈 화면 소개와 레벨 카드가 단어 대신 **줄 수**를 보여줍니다
- **`packBases` 캐시** — 팩 객체당 한 번만 계산합니다. 수만 줄짜리 팩을 매 호출마다 훑으면 언어 선택 화면이 멈춥니다

### 배포 전 검토에서 잡아 고친 것
- **이어읽기가 아예 안 돌았다** — `levelView` 가 고른 탭을 `view.tab` 에 되돌리지 않아, 읽기만 있는 팩(기본 탭이 읽기)에서 스크롤 추적이 매번 바로 빠져나갔습니다. 서재 원서가 정확히 그 모양이라 핵심 기능이 통째로 죽어 있었습니다
- **다 읽고 내려가면 위치가 지워졌다** — 모든 줄이 화면 위로 지나가면 `top` 이 0 으로 남아 저장된 위치를 덮어썼습니다. 이제 마지막 줄로 보거나 저장하지 않습니다
- **팝업의 🔊·🔎 버튼이 눌리지 않았다** — 바깥 클릭 감지를 `pointerdown` 으로 걸어 둔 탓에, `click` 보다 먼저 도착해 버튼을 DOM 에서 떼어내었습니다. ⚙ 메뉴가 쓰는 방식(대상 검사)과 같게 맞췄습니다
- **새로고침하면 보던 원서를 잃었다** — `libRestore()` 가 비동기라 부팅 시점에는 그 팩이 없는데, 활성 팩 폴백이 먼저 돌아 id 를 뺏고 다음 `save()` 가 그걸 굳혔습니다. 서재에 알려진 id 는 복원이 끝날 때까지 지킵니다
- **더블클릭할 때 번역이 깜박였다** — `dblclick` 앞에 `click` 이 두 번 먼저 오므로 줄 토글이 두 번 돌았습니다. 한 번 클릭 토글을 220ms 미뤄 두고 더블클릭이 오면 취소합니다
- **다른 팩에서 찾은 낱말을 엉뚝한 음성으로 읽었다** — 팝업의 🔊 가 읽는 쪽 팩의 로케일을 썼습니다. 이제 뜻을 가져온 팩의 로케일로 읽습니다(통합검색과 같은 방식)
- 렌더 뒤에 늦게 터지던 검색 예약 정리, 받는 중 화면을 나갈 때의 `libState` 널 참조 방어

### 측정치 (3,000줄 기준)
| 항목 | 값 |
|---|---|
| 렌더 | 107ms |
| 줄 이동 | 7~36ms (`content-visibility` 적용 전 541ms) |
| 글 안 검색 스캔 | 4ms |
| 사전 맵 생성 | 19ms (5,011단어) |

---
## v7.0.0.0 — 2026-08-24
**LANG · 🏛 라틴어 신설 (7번째 언어) + 📜 읽기(원문 강독) 포맷 도입**
- **`la.js`** — 고전 라틴어 본과정. 문법 16단계 계획 중 **1~6레벨 완성**(102단어)
  - L1 소리와 여섯 격 / L2 1변화·sum / L3 1활용 동사 / L4 3변화 명사 / L5 형용사 두 부류·i어간 / L6 4·5변화·대명사
  - 지난 두 버전에 넣은 확장점이 전부 실제로 쓰임: `say`(고전식 발음) · `ignoreDiacritics`(마크론) · `answers`(어순 자유) · `phaseLabels` · `columnLabels`(발음 열 → **주요부**)
- **발음 재생 방식** — 브라우저에 `la` 음성이 없어, 표기와 별개로 `say`에 변환 철자를 담아 **이탈리아어 음성**으로 근사
  - 변환: `c→k` `v→w` `ae→ai` `oe→oi` `ph th ch→p t k` `y→i`, **`qu`·`ll`·`tt`·`pp`는 그대로**
  - 실제로 들어보고 이탈리아어를 고름: 이중자음이 살아 있고(mittō·oppidum), `ll`이 /l.l/이며(스페인어는 /ʎ/라 puella가 "푸에야"), `qu`를 /kw/로 읽고, `w`를 외래어식 /w/로 읽어 v를 재현할 수 있음
  - ⚠️ 이중모음 치환은 마크론 제거보다 **먼저** 해야 함 — `poēta`는 po-ē-ta이지 oe 이중모음이 아님(`poeta` ○ / `poita` ×)
- **📜 읽기 탭 신설** — 레벨에 `reading`을 두면 탭이 자동 생성. 원전 강독·장문 독해를 담는 자리
  - `lines[]`에 원문 `t` / 번역 `m:{kr}` / 낱말 주석 `note[]` / 발음용 `say` / 문단 구분 `p`
  - 번역·주석은 **기본으로 감춰** 가려 읽기가 되고, 줄을 누르면 펼쳐짐. 상단 버튼으로 전체 토글(선택은 기기에 저장)
  - 라틴어 3개 자료 21줄 수록 — 로마 격언, 「Vīta brevis, ars longa」, 「Vēnī, vīdī, vīcī」
  - 사전 팩에도 쓸 수 있어, 그동안 "지문이 길어 구조와 안 맞는다"고 미뤄둔 **JLPT 독해**도 이 포맷으로 담을 수 있음
- 새 언어 추가로 **LANG 6→7** → v7.0.0.0

## v6.3.1.0 — 2026-08-24
**MINOR · 호환성 6건 정비 — 실측으로 찾은 문제를 라틴어 착수 전에 선처리**
- **통합검색 인덱스 캐시 + 디바운스**: 매 키 입력마다 전 팩을 `norm()` 하던 것을 미리 정규화한 인덱스로 교체. 측정값 **키당 54ms → 0.006ms**(100회 0.6ms), 입력 렌더는 120ms 디바운스. 팩 개수가 바뀌면 인덱스 자동 재생성
- **작문 채점의 "공백=단어" 가정 제거**: 한중일·태국·크메르·라오 문자가 섞이면 글자 수로 판정(`这是我的书。` 통과, `书。` 차단). 팩이 `minChars`로 직접 지정 가능
- **종결부호 확대**: `.!?` 만 보던 것을 `。！？؟۔；;…` 까지. 팩이 `endPunct`로 지정하거나 `checkEnd:false`로 끌 수 있고, 대문자 검사도 `checkCase:false`로 해제 가능
- **SRS 키 충돌 해소**: 표제어 문자열만 쓰던 키를 `L{레벨}|{표제어}`로 분리(팩이 `w.id`를 주면 우선). 측정 결과 기존 데이터에 **중복 표기 150건**이 있어 진도가 서로 덮어써지고 있었음(`ぶらぶら`가 3개 팩에 존재). 옛 키도 계속 읽어 진도는 보존
- **발음 구별 부호 선택적 무시**: 팩 `ignoreDiacritics:true` 면 채점 시 결합문자를 제거(라틴어 `amāre`=`amare`). 프랑스어 등 악센트가 철자인 언어는 기본값 유지
- **blocks 복수 정답**: `answers` 배열 지원. 어순이 자유로운 언어에서 `Rosam puella amat`도 정답 처리
- **`say`·`exSay` 필드 신설**: 화면 표기와 발음용 표기를 분리. 라틴어 고전식(`Caesar`→`Kaisar`)을 스페인어 음성으로 근사하는 용도이며, **한자·JLPT 사전 팩에 소급 적용**(`呵`→`가`, 한자 표기→요미) — 그동안 TTS가 표기를 그대로 읽어 발음이 어긋나던 것 교정 (한자 1,607 · N3 521 · N1 887 항목)

## v6.3.0.1 — 2026-08-24
**PATCH · 엔진 범용성 정비 — 임의 콘텐츠(RTL 언어·인공어·비언어 과정)를 팩만으로 수용**
- **tts 없는 팩 지원**: `tts` 필드를 생략하면 듣기 열·발음 버튼(단어표/단어은행/통합검색/플래시카드/복습/연습 4종)이 전부 자동 숨김. 기존엔 `speak()`가 `lang.split` 에서 크래시 — 플래시카드 뒤집기만 해도 죽던 버그 수정
- **RTL 자동 지원**: 학습 언어 텍스트 요소에 `unicode-bidi:plaintext`, 입력창에 `dir="auto"` — 아랍어·히브리어 팩이 설정 없이 올바른 방향으로 표시
- **`targetName()` 폴백 체인**: 엔진 TARGETS 표 → 팩 `names:{kr,en,…}` → 팩 `name` → 코드. 새 target 코드('ar'·'solresol'·'piano' 등)를 엔진 수정 없이 추가 가능. `gsPackLabel`도 동일 경로로 단순화
- **홈 서브타이틀 데이터 주도화**: "CEFR 기반 10레벨 커리큘럼" 하드코딩 제거 → 팩 `desc` 우선, 없으면 "레벨 N개 과정" 자동 생성. EN base에서 한국어로 나오던 문제도 함께 수정. fr·es·zh 팩에 `desc` 보강
- **레벨 카드**: `cefr` 없는 레벨에서 "LEVEL 3 · " 처럼 구분점이 매달리던 표기 수정
- **시험 탭**: `quiz` 없는 레벨에서 시험 시작 시 빠져나올 수 없던 문제 → "시험이 없습니다" 안내. Library 모드와 어긋나던 "Level N+1이 열립니다" 문구를 "통과입니다(자기 점검용)"로 정정
- **`L.words` 누락 하드닝**: 문법만 있는 레벨이 있어도 홈/단어표/플래시카드/단어은행이 죽지 않음 (5개 지점 `||[]`)
- **UI 문구 전면 i18n**: 채점 피드백("✅ 정답입니다!"·"❌ 정답은 …")·시험 화면(제목·안내·점수·통과/실패·재응시)·연습 버튼(확인·다시·듣기)·작문 첨삭 메시지·플래시카드 완료·빈 상태 안내·헤더 툴팁·언어팩 가져오기 alert까지 **60여 개 문자열을 I18N 테이블로 이관**. EN base에서 한국어가 새어 나오던 문제 해소
  - 자리표시자 치환 헬퍼 `TF(key, vars)` 추가 — `quizIntro:'{c}문항 · {s}점 이상…'` 처럼 수치를 문구에 끼워 넣음
- **`phaseLabel` 데이터 주도화**: 팩이 `phaseLabels:{intro:'Warm-up', A:'Etudes'}` 로 단계 이름을 직접 공급 가능. 없으면 base 언어 기본 라벨, 정의되지 않은 phase 값은 그대로 표시 — 프랑스어 전용이던 "Phase A · 블록 조립"에 다른 과정이 묶이지 않음
- `_template.js`·README에 새 규칙 문서화 (`names`·`desc`·`phaseLabels`·tts 생략·RTL 자동)

## v6.3.0.0 — 2026-08-23
**MAJOR · 🇯🇵 JLPT N3·N1 단어 사전 신설**
- `jlpt-n3.js` / `jlpt-n1.js` — 단어장 어휘를 **오십음도(あ~わ행) 10분류**로 재정렬한 열람 전용 사전. N3 **622단어** · N1 **1,116단어**
  - OCR 전수 검수 완료: 1,738행 중 **162건 교정**(작은 가나 93 · 뜻 깨짐 38 · 표제어 17 · 요미 11 · 기타 3). 자동 규칙 오탐 12건은 되돌림(`自由 じゆう`·`理由 りゆう`·`石油 せきゆ` 등 원래 큰 가나가 맞는 낱말)
- 일본어 카드 ▾ 에서 접근

## v6.2.0.0 — 2026-08-23
**MAJOR · 🇯🇵 일본어 본과정(CEFR A1~B1 10레벨) 신설 + JLPT N1 문자·어휘**
- `ja.js` — 빈 껍데기였던 일본어 카드에 **10레벨 219단어** 본과정을 채움. 프랑스어와 같은 깊이
  - L1 가나·인사 / L2 숫자·자기소개(です) / L3 こそあど·ある・いる / L4 い·な형용사 / L5 ます형·조사 / L6 て형 / L7 보통형·た형 / L8 인용·추측 / L9 가능·수동·사역·조건 / L10 경어
- `jlpt-n1-vocab.js` — JLPT N1 문자·어휘 **출제 유형 4가지**(한자읽기·문맥규정·유의표현·용법)를 따른 **창작 문항 48개**
- 일본어 카드 ▾ 에서 일본 음식·오노마토페와 함께 접근

## v6.1.0.0 — 2026-08-20
**MAJOR · 🇰🇷 1급 배정한자 사전 신설 (1,607자 전수 수록)**
- `hanja1-dict.js` — 대한상공회의소 「배정한자(1~4급)」 자료의 **1급 열 전체 1,607자**를 수록. 공식 표기 자수와 정확히 일치
- 1급 배정한자는 2·3·4급과 겹치지 않는 별도 집합(교차 검증 결과 중복 0자) — 이 사전이 곧 "2급에 없는 1급 신규 한자" 전부
- 초성 ㄱ~ㅎ **14개 분류**, 각 항목 `한자 / 음 / 훈(뜻)`. `dictionary:true` — 시험·연습·잠금 없이 열람 전용
- 표 열 추출 검증: 4급 300자 · 3급 900자 · 2급 1,501자 · 1급 1,607자 — 네 급수 모두 문서 표기 자수와 일치
- 원본 PDF가 유니코드 매핑 없이 폰트 전용 글리프(PUA)로 넣은 12자는 CJK 확장B 코드포인트로 복원해 수록:
  𣃽(나) 𢻠(리) 𪊭(린) 𩔉(민) 𩠻(별) 𥜽(설) 𤨒(은) 𥌾(응) 𩓞(정) 𩇕(정) 𡽜(한) 𠘕(희)
  - 확장B 영역이라 기기 폰트에 따라 네모(두부)로 보일 수 있음
  - `𩓞`·`𩇕`은 원본 표에서 '조' 그룹 끝에 놓여 있으나 음은 '정'이 맞아(PDF 배열 오류) '정' 그룹에 수록
- "상공회의소 한자" 카드에서 `1급 배정한자`로 진입. 기존 1·2·3급 학습 과정과 별개
- 단어은행·통합검색에서도 함께 검색됨

## v6.0.0.0 — 2026-08-20
**LANG · 🇰🇷 대한상공회의소 한자 신설 — 한 카드 안에 1·2·3급 과정**
- 언어 카드는 **"상공회의소 한자" 하나**, 카드의 **▾** 로 3급·2급·1급을 펼쳐 바로 진입. 각 급수는 독립 팩이라 **진도·시험 점수가 서로 분리**됨: `hanja3.js`(3급, 5레벨) · `hanja2.js`(2급, 4레벨) · `hanja1.js`(1급, 4레벨)
- 카드 본체를 누르면 1순위 과정(3급)으로 redirect — `hanja.js`(빈 껍데기) + `extra:true` 하위 팩 구조 사용(엔진 수정 없이 기존 기능 재사용)
- 상공회의소 **배정한자**(3급 900자 / 2급 1,501자 / 1급 1,607자) 기준으로 대표 한자를 훈·음·용례와 함께 수록
- 시험·연습은 상공회의소 출제 유형 그대로: **제자원리(造字 방식이 같은 한자 찾기)** · **성어 뜻풀이 고르기 / 뜻에 맞는 성어 고르기** · **훈이 비슷한 한자 찾기**
- 문법 노트: 육서(象形·指事·會意·形聲) 구별법, 성부(聲符)로 음 짐작하기, 부수로 뜻 갈래 잡기, 뜻이 비슷한 한자 짝 정리
- 표 열 이름을 팩에서 지정: `columnLabels:{word:'한자', ipa:'음', meaning:'훈(뜻)', ex:'용례'}`
- fix: 채점 후 900ms 타이머 도중 화면·언어를 옮기면 `quiz`가 null이 되어 나던 콘솔 에러에 가드 추가
- 언어 1개 추가로 **LANG 5→6** → v6.0.0.0

## v5.3.0.1 — 2026-08-04
**PATCH · 프랑스어 확장 사전 3개 분류 추가 (실용 동사·여행·비즈니스)**
- `🧾 실용·수속 동사` 22개: annuler·réserver·confirmer·louer·essayer·vérifier·signer 등 (일상 실용 동사)
- `🧳 여행·호텔` 21개: hôtel·aéroport·billet·passeport·réservation·réception·petit-déjeuner·addition·complet 등
- `🤝 비즈니스·격식` 18개: entreprise·réunion·contrat·devis·délai + 격식 표현 Veuillez·Pourriez-vous·Cordialement·Ci-joint·N’hésitez pas à·Dans l’attente de votre réponse (register 팁 첨부)

## v5.3.0.0 — 2026-08-04
**MAJOR · 🔎 통합검색 신설 — 설치된 모든 언어팩을 한번에 검색**
- 헤더에 **🔎 Search** 버튼 추가(다시 누르면 학습 홈으로 — 단어은행 버튼과 동일한 토글 방식)
- 활성 언어와 무관하게 **설치된 모든 언어팩**(빈 껍데기 제외)의 단어·발음·뜻·예문을 한번에 검색
- 결과를 누르면 **그 언어로 전환 + 해당 레벨의 단어 탭**으로 바로 이동, 발음 듣기 버튼도 각 언어의 TTS로 재생
- `App.switchLang(id, nav)`에 선택적 `nav` 인자 추가(전환 후 특정 화면으로 이동) — 기존 호출부는 영향 없음

## v5.2.1.0 — 2026-07-18
**MINOR · 표 열 이름·순서를 팩에서 지정(`columnLabels`·`columnOrder`)**
- 팩이 **아무 열이나 이름 변경**: `columnLabels:{ ipa:'한자', meaning:'뜻풀이', … }` (key = `cat`·`word`·`ipa`·`meaning`·`ex`·`speak`)
- 팩이 **열 순서 변경**: `columnOrder:['word','meaning','ipa','ex','speak']` — 목록에 없는 열은 원래 순서로 뒤에 붙음
- 적용: 고사성어 사전은 ipa 자리에 한자를 넣으므로 `columnLabels:{ipa:'한자'}` → 열 이름 **"발음"→"한자"**. 일괄 적용이 아니라 팩별 지정이라 일본 음식(요미가나)·중국어(병음)·프랑스어(IPA)의 발음 열은 그대로 유지
- `_template.js`에 두 필드 설명 추가

## v5.2.0.0 — 2026-07-18
**MAJOR · 🇰🇷 고사성어 사전 신설 + 🇯🇵 오노마토페 확장**
- **`kr-idiom.js`** (한국어 하위 extra 사전): 한자 고사성어 **70개 7분류** — 노력·인내(대기만성·와신상담), 어리석음(오리무중·조삼모사), 우정(관포지교·수어지교), 처세·인과(새옹지마·자업자득), 위기(사면초가·배수지진), 학문(청출어람·괄목상대), 언행(촌철살인·교언영색). 각 성어에 한자 표기·뜻·**유래(고사) 팁** 첨부
- **`ja-onomatope.js` 확장** (+33개 3분류 → 총 191개): 🖐️감촉(ざらざら·すべすべ·もふもふ), 👃냄새·자극(ぷんぷん·こんがり·ぴりぴり), 💫만화 효과음(ドーン·ゴゴゴ·シーン·ズキューン)
- 새 단어팩(고사성어) 추가 규칙에 따라 **MAJOR 자리 +1** → v5.2.0.0

## v5.1.0.0 — 2026-07-18
**MAJOR · 🇯🇵 일본어 의성어·의태어(오노마토페) 사전 추가**
- **`ja-onomatope.js`** (일본어 하위 extra 사전): 擬音語·擬態語 **158단어 12분류** — 감정(ドキドキ·イライラ), 웃음·말투(にこにこ·ぺらぺら), 빛·윤기(キラキラ·ピカピカ), 날씨(ザーザー·じめじめ), 동물 소리(ワンワン·コケコッコー), 식감(もちもち·さくさく·とろとろ), 움직임(てくてく·ぶらぶら), 소리·충돌(ガタガタ·ドンドン), 아픔·몸(ずきずき·へとへと), 잠(ぐうぐう·すやすや), 성격·정도(てきぱき·ぴったり), 놀람(はっと·ぞっと)
- 각 단어에 로마자 읽기·예문·한국어 뜻, 뉘앙스 팁(ドキドキ vs ワクワク, こってり↔あっさり 등) 첨부. 일본어 카드 ▾ / Contents에서 접근
- 새 단어팩(메뉴) 추가 규칙에 따라 **MAJOR 자리 +1** → v5.1.0.0

## v5.0.0.0 — 2026-07-18
**LANG · 🇨🇳 중국어 추가 (5번째 언어)**
- **중국어 학습팩 `zh.js`** (base 한국어): 성조·인사 / 숫자·자기소개 / 가족 / 음식·식당 / 쇼핑·시간 5레벨. 간체자 + 병음(성조부호) + 예문, 레벨별 문법 노트·연습·시험 포함(是·吗·有·양사 个·喜欢/想·多少钱 등)
- **중국 현지 회화 사전 `zh-local.js`** (extra:true, 9분류 96단어): 교과서에 없는 실전 표현·신조어·밈 — 리액션(加油·绝了·真的假的), 인터넷/SNS(yyds·666·热搜), 직장·태도(内卷·躺平·摸鱼·996), 감정·멘탈(破防·社死·emo), 음식/결제(外卖·AA制·不要香菜), 이동/생활(打车·高铁·快递), 호칭(帅哥·老板·老铁), 연애(脱单·暧昧·单身狗), 쇼핑·흥정(打折·砍价·剁手) — 문화 팁 다수 첨부
- 중국어는 KR base 전용(뜻이 한국어만) → 언어 선택에서 KR 모드에만 노출. 카드 ▾ 로 현지 회화 사전 접근
- 새 언어 추가로 **LANG 4→5**, 나머지 자리 0으로 리셋 → v5.0.0.0

## v4.2.1.0 — 2026-07-18
**MINOR · 언어 비종속 단어 스키마(m:{base}) + target당 1파일 통합** ⚠️ 팩 규격 변경
- 단어 뜻을 base로 키잉: `ko`/`en` → **`m:{kr,en}`**, `exKo` → `exm:{kr}`, `tip` → `tip:{kr}`. `w`·`ipa`·`ex`·`gender` 등 base 무관 필드는 그대로. base 추가 = 각 단어 `m`에 키 하나 추가
- 팩은 이제 **target만 선언**(base 필드 폐지). 팩이 지원하는 base는 단어 `m`의 키로 자동 결정. 파일명도 target 기준으로 통합: `kr-fr`+`en-fr`→**`fr.js`**, `kr-es`+`en-es`→`es.js` 등. 파생 파일(en-*.js) 폐지 → 데이터 중복·불일치 없음
- 엔진이 base로 뜻을 골라 렌더(`meaningOf`/`exmOf`/`tipOf`). KR 모드는 한국어 뜻만, EN 모드는 영어 뜻만 표시(반대편 감춤)
- 언어 선택 화면은 **현재 base로 뜻이 있는 target만** 노출(예: 한자어는 kr 전용이라 EN에선 숨김. 일본 음식은 영어 뜻이 있어 EN에도 노출)
- 향후 확장 대비 `registerMeanings(target, base, map)` 훅 추가 — A(인라인)로 시작해도 나중에 표제어키 오버레이 파일로 무손실 전환 가능

## v4.2.0.1 — 2026-07-16
**PATCH · 일본어·한국어 사전을 빈 껍데기+확장 구조로 복원**
- v4.2.0.0에서 사라졌던 **빈 언어 껍데기 + 확장 사전 + ▾ 드롭다운 + redirect** 구조 복원(v4.1.3.0 동작)
- `kr-ja.js`·`kr-kr.js`는 `levels:[]` 빈 껍데기(일본어·한국어), 사전은 `kr-ja-food.js`(일본 음식)·`kr-kr-hanja.js`(한자어)로 분리하고 `extra:true` 로 하위 확장 처리
- 언어 선택 카드의 **▾** 로 확장 사전을 펼치거나, 빈 껍데기 카드를 누르면 **1순위 확장 사전으로 자동 redirect**(`resolvePack`). Contents 드롭다운은 빈 껍데기를 건너뛰고 사전만 표시
- 한국어 target 코드를 `ko`→`kr`로 통일(`kr-kr`)

## v4.2.0.0 — 2026-07-16
**MAJOR · Base→Target 언어 체계 도입 (EN→FR/ES) + Library화** ⚠️ 팩 규격 변경
- **base(설명 언어) · target(학습 언어) 2축 도입**. 팩 id = `{base}-{target}` (예: `kr-fr`, `en-fr`). 언어 선택 화면 상단 **"Languages" 옆 Base 드롭다운**(한국어/English)으로 전환
- **파일 재편**: `fr→kr-fr`, `fr-extra→kr-fr-extra`, `es→kr-es`, `ja-food→kr-ja`, `hanja-words→kr-ko`. 지난 빈 껍데기 `ja.js`/`kr.js` 삭제
- **EN→FR / EN→ES 신설**: `en-fr.js`/`en-es.js`가 kr 팩에서 **자동 파생**(`registerDerivedPack`) — 각 단어의 `en` 뜻만 쓰는 **어휘 열람 전용**. 문법·연습·시험 데이터는 없음
- **뜻 표시가 base를 따름**: KR 모드는 한국어 뜻만, EN 모드는 영어 뜻만 (KR→FR에서 영어 안 보임)
- **UI 언어 = base**: base=English면 탭·메뉴(Words/Flashcards/Grammar/Practice/Test, Display/Theme…)와 헤더가 영어로. `I18N` 문자열 테이블 추가
- **Library화(잠금만 해제)**: 모든 레벨 잠금 해제 → 자유 열람. 시험·암기카드·문법·연습 탭은 유지(시험은 선택 연습)
- 버전 규칙상 **새 번역 방향 = MAJOR 자리** → 4.1.3.0 → **4.2.0.0** (LANG=학습 언어 4 유지)
- `⚠️ 팩 규격`: 팩에 `base`·`target` 필드 추가(미지정 시 base='kr'), 확장 팩은 `extra:true`, 파생은 `registerDerivedPack(원본id, {id,…})`

## v4.1.3.0 — 2026-07-16
**MINOR · 일본어·한국어를 상위 언어로 승격 + 빈 언어 redirect** ⚠️ 팩 규격 변경
- **일본어(ja)·한국어(kr) 상위 언어 팩 신설**(`levels:[]` 빈 껍데기). 기존 `일본 음식`→`parent:'ja'`, `한자어`→`parent:'kr'` 로 하위 사전화
- 언어 선택 화면: 일본어·한국어 카드에 **▾ 드롭다운**(각각 일본 음식 / 한자어 사전). **본과정이 비어 있는 언어는 카드를 누르면 1번 확장 사전으로 자동 redirect** (`resolvePack`)
- 최상위 언어 수는 4개 유지(프랑스어·스페인어·일본어·한국어) → LANG 자리 변동 없음. 나중에 `levels` 를 채우면 그대로 본과정이 생김
- `⚠️ 팩 규격`: 빈 상위 언어는 `levels:[]` 로 등록, 하위 콘텐츠는 `parent:'<상위 id>'` 지정

## v4.1.2.0 — 2026-07-16
**MINOR · 확장 드롭다운을 "언어 선택" 페이지로 이동**
- 언어 선택 페이지(picker)의 각 언어 카드에 **▾ 드롭다운** 추가 → 확장/사전 콘텐츠를 펼쳐 **바로 그 콘텐츠로** 진입
- 확장이 없는 언어(스페인어)는 ▾ 없이 카드 클릭만. 자체가 사전인 최상위 팩(일본 음식·한자어)은 클릭 시 곧장 그 사전으로 진입(redirect)
- **Contents 드롭다운은 v4.1.0.0 평면 방식으로 복원** — v4.1.1.0에서 넣었던 Contents 내부 ▾ 중첩은 제거하고, 언어별 펼침 UI는 선택 페이지 쪽으로 일원화

## v4.1.1.0 — 2026-07-16
**MINOR · Contents 중첩 드롭다운 + 헤더 라벨 대문자화**
- Contents 메뉴에서 언어 본과정 버튼 오른쪽에 **▾** 캐럿 추가 → 눌러서 확장/사전 팩을 **중첩 목록으로** 펼침·접기 (현재 확장 팩을 쓰는 중이면 자동으로 펼쳐짐)
- 기존(v4.1.0.0) **평면 목록 접근 방식도 그대로 유지** — 확장 사전은 캐럿 펼침과 평면 항목 양쪽에서 접근 가능
- 헤더 버튼 첫 글자 대문자화: word→**Word**, remind→**Remind**, menu→**Menu** (Contents는 기존 그대로)

## v4.1.0.0 — 2026-07-16
**언어 선택 페이지 + 📂 Contents 드롭다운 · 헤더 라벨 영어화 · 4자리 버전 체계 확정** ⚠️ 팩 규격 변경
- **언어 선택 페이지 신설**: 헤더의 언어 `<select>` 드롭다운을 제거하고, 언어를 카드로 고르는 별도 화면(`picker`)으로 분리 — 언어가 늘어도 드롭다운이 넘치지 않음
- **📂 Contents 드롭다운**: 현재 언어의 콘텐츠(본과정 + 확장/사전 팩)를 전환하고, "🌐 언어 선택…"으로 선택 페이지 진입. 기존 언어 드롭다운을 대체
- **`fr-extra` 확장 팩을 프랑스어 하위로 이동**: `parent:'fr'` 지정 → 언어 선택엔 안 뜨고 프랑스어의 Contents에서 접근. `⚠️ 팩 규격 변경`: 팩에 선택적 `parent`(상위 언어 id)·`menuLabel`(드롭다운 표기) 필드 추가
- **헤더 라벨 영어화**: 단어은행→**word**, 복습→**remind**, 메뉴→**menu**, 새 버튼 **Contents** (본문·푸터 등 나머지 한국어 유지 — 현재는 "한국어→타 언어" 구성)
- **버전 체계를 `LANG.MAJOR.MINOR.PATCH`로 확정**(위 규칙표): LANG=언어 수(4), MAJOR=대형 기능(단어은행=1). 직전 임시 `v5.0.0`/`v4.2.0.0` 표기를 이 기준으로 **v4.1.0.0**으로 재정렬

## v4.3.1 — 2026-07-10
**PATCH · 표 열 접기 + 팁 숨김**
- 표 위 "열 접기" 칩으로 원하는 열(분류·발음·뜻·예문·듣기)을 접었다 펼 수 있음 (단어 열은 항상 표시)
- 뜻 열 안의 **💡팁**도 별도 칩으로 숨김 가능
- 선택은 localStorage에 저장되어 새로고침·재접속 후에도 유지. 데이터에 없는 열/팁은 칩에도 표시되지 않음

## v4.3.0 — 2026-07-10
**MINOR · 한국어 줄바꿈을 어절 단위로**
- `word-break: keep-all` 적용 → 한국어 텍스트가 글자 단위가 아니라 **어절(공백) 단위로만** 줄바꿈됨 (단어가 중간에 끊기던 문제 해결)
- 표·카드·설명 등 앱 전역에 적용. 라틴 문자(프랑스어/스페인어)는 기존대로 공백 단위로 끊기고, 페이지 가로 오버플로우 없음(표만 스크롤)

## v4.2.0 — 2026-07-10
**MINOR · 표를 데이터 주도로 (호환성 강화)**
- 언어팩 데이터에 **값이 하나도 없는 필드는 해당 열 자체를 렌더하지 않음**
  (예: 한자어 팩처럼 `ex`/`exKo`가 모두 `''`이면 "예문" 열이 사라짐)
- 발음(`ipa`), 뜻(`ko`/`en`/`tip`), 예문(`ex`) 열 모두 동일 규칙 적용 — 새 언어팩이 어떤 필드를 채우든 표가 자동으로 맞춰짐
- 레벨 단어 표와 단어은행 표가 공통 렌더러(`wordTableHtml`)를 쓰도록 통합
- 열 개수에 맞춰 표 최소 너비를 계산 → 열이 적은 팩은 모바일에서 불필요한 가로 스크롤이 생기지 않음
- 카드 모드에서도 빈 `ko`/`exKo`가 빈 줄로 남지 않도록 정리

## v4.1.0 — 2026-07-10
**MINOR · 표 열 너비 우선순위 개선**
- 단어 표에서 "발음" 열이 `[ 안녕 ]`처럼 줄바꿈되던 문제 수정 — 짧은 열(분류·단어·발음·듣기)은 내용 너비에 딱 맞추고(nowrap), 남는 폭은 "뜻"·"예문" 열이 가져가도록 변경
- 레벨 단어 표와 단어은행 표 모두 적용. 모바일 가로 스크롤은 그대로 유지

## v4.0.3 — 2026-07-10
**PATCH · 업데이트가 웹에 안 뜨던 문제 수정**
- 서비스워커가 앱 셸을 프리캐시할 때 브라우저 HTTP 캐시(GitHub Pages max-age)를 우회하도록 `{cache:'reload'}` 적용 → 버전을 올리면 언어팩 등 새 내용이 확실히 반영됨
- 런타임 백그라운드 갱신도 서버 재검증(no-cache)으로 변경

## v4.0.2 — 2026-07-10
**PATCH**
- 한자 단어 추가

## v4.0.1 — 2026-07-10
**PATCH**
- Changelog와 버전 수정
- 한자어 사전에 발음 추가

## v4.0.0 — 2026-07-10
**MAJOR · 4번째 언어팩(한자어 사전) 추가**
- 일상에서 사용하는 한자어와 불교 유래 단어들에 대한 사전 추가

## v3.3.1 — 2026-07-10
**PATCH**
- 일본 음식 사전에 몇가지 단어 추가

## v3.3 — 2026-07-10
**MINOR · 3.0 이후 세 번째 사용성 업데이트**
- 📚 단어은행 버튼이 **토글**로 동작: 한 번 누르면 단어은행, 다시 누르면 학습 홈으로 (PC에서 좌측 로고까지 이동할 필요 없음)
- 단어은행에 **🃏 카드 모드** 추가: 원어만 보이는 카드를 누르면 발음·뜻·예문이 아래로 펼쳐짐 (표/카드 선택은 기기에 저장)

## v3.2 — 2026-07-10
**MINOR · 3.0 이후 두 번째 사용성 업데이트**
- 📚 **단어은행** 추가: 헤더 버튼 하나로 현재 언어팩의 모든 레벨/분류 단어를 한 화면에서 검색·필터
  (실시간 검색, 분류 칩 필터, 학습팩·사전팩 공통 지원)
- 버전 표시 체계 도입: ⚙ 메뉴 하단 + 하단 footer에 현재 버전 노출

## v3.1 — 2026-07-09
**MINOR · 3.0 이후 첫 사용성 업데이트**
- 모바일에서 단어 표가 화면 밖으로 삐져나가던 문제 수정 → 표 가로 스크롤 컨테이너 적용
- 다크 / 라이트 모드 수동 전환 추가 (⚙ 메뉴, 시스템 자동 감지는 기본값으로 유지)
- 헤더의 "＋ 언어팩" 버튼을 **⚙ 메뉴 드롭다운**으로 교체(테마 전환 포함), 좁은 화면에서 헤더가 넘치던 문제 함께 수정

## v3.0 — 2026-07-09
**MAJOR · 세 번째 언어팩(🇯🇵 일본 음식) 추가**
- 일본 음식 사전 팩(`ja-food.js`) 추가 — 생선·해산물, 고기, 채소·버섯, 조리법, 조미료·양념, 요리·메뉴, 밥·면·주식, 라멘·토핑(지로계 포함) 8개 분류 72단어
- 엔진에 **`dictionary:true` 모드** 신설: 시험·연습·레벨 잠금 없이 "열람 전용" 언어팩을 지원 (사전류 콘텐츠에 적합)
- Notion "🇯🇵 일본 음식 단어장" 데이터베이스와 동기화

## v2.1 — 2026-07-08
**MINOR · 이름 변경 및 설치형 웹앱(PWA) 전환**
- Polyglot Builder → **3Ls (Language Learning Library)** 로 리브랜딩
- PWA 전환: `manifest.json` + `service-worker.js` + 아이콘 — 홈 화면 설치, 오프라인 지원
- GitHub Pages 배포 (`pocabo.github.io/3ls`), 자동 업데이트(서비스워커 stale-while-revalidate) 도입

## v2.0 — 2026-07-07
**MAJOR · 두 번째 언어팩(🇪🇸 스페인어) 추가**
- 스페인어 학습팩(`es.js`) 추가 — 프랑스어와 동일한 10레벨 CEFR 구조
- ser/estar 구분, 어간모음변화 동사, gustar 역구조 등 스페인어 고유 문법 반영
- Notion "🇪🇸 스페인어 단어책" 데이터베이스 동기화

## v1.0 — 2026-07-07
**MAJOR · 최초 릴리스 — 학습 엔진 + 🇫🇷 프랑스어**
- 데이터 주도형(language-agnostic) 학습 엔진 최초 구현
- 프랑스어 10레벨 CEFR 커리큘럼: Phase A(블록 조립)·B(빈칸/변형)·C(자유 작문)
- 마일스톤 시험(80점 해제), 누적 복습 SRS, TTS 발음, 특수문자 키보드
- Notion "🇫🇷 프랑스어 단어책" 데이터베이스 동기화
